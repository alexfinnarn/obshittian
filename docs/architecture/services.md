# Services

This page is retained as a pointer only.

Service behavior is documented in:

- [overview.md](overview.md) for client/server boundaries and bootstrap orchestration
- [../developer-guide.md](../developer-guide.md) for the practical development workflow

Current note:

- `fileService` is the browser-side boundary for filesystem access and talks to `/api/files/*`
