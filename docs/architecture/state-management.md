# State Management

This page is retained as a pointer only.

State ownership and persistence are documented in:

- [overview.md](overview.md) for store responsibilities and bootstrap order
- [../reference/storage-contracts.md](../reference/storage-contracts.md) for vault-owned files and localStorage keys

Current note:

- `vaultConfig` includes `dailyTasks` (simplified to `id`, `name`, `days`) in addition to quick links and quick files
