# Actions

This page is retained as a pointer only.

The architecture docs were consolidated to reduce drift. Use:

- [overview.md](overview.md) for runtime flow, boundaries, and event coordination
- [../developer-guide.md](../developer-guide.md) for local development workflow

Current note:

- Svelte actions in this codebase are small DOM-behavior helpers, not a separate architectural layer with its own source of truth
